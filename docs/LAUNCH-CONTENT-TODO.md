# FACING 론칭 콘텐츠 TODO — 실행 리스트 (SSOT)

> 작성: 2026-06-10 23:55 · 1차 감사(`CONTENT-LAUNCH-AUDIT-2026-06-10.md`)를 **자기반박 2차 검증**으로
> 교정한 최종 실행 목록. 1차와 판정이 달라진 항목은 §6 정정 기록에 명시.
> 표기: `[C]` = Claude 가 즉시 실행 가능 / `[U]` = 사용자 액션 필요 / DoD = 완료 판정 기준.
> 진행 규칙: 클러스터 단위 로컬 커밋 (push 금지 유지). 완료 시 본 문서 체크박스 갱신.

---

## 진행 현황판 — 2026-06-11 01:10 전 항목 종결

| 등급 | 총 | 완료 | 잔여 |
|---|---|---|---|
| 🔴 P0 | 5 | **5** | P0-5 [U] NHN 키 발급만 사용자 액션 (코드·고지 측은 완료) |
| 🟠 P1 | 8 | **8** | P1-8 스크린샷·스토어 등록만 배포 방식 결정 후 |
| 🟡 P2 | 11 | **11** | 없음 — 2차 패스에서 잔여 4건 전부 처리 (아래) |

### 2차 패스 (2026-06-11 — "전부" 지시로 잔여 4건 종결)
- **P2-1 FAQ**: 시드 10문답 작성 (`faq_screen.dart` — 코드로 검증된 동작만 기술,
  추측 0건. 실문의 누적 시 증보 원칙은 파일 헤더에 명시) + Profile 진입 버튼
- **P2-2 재활**: "배지 유지" 로 종결 — 콘텐츠 원천이 rahap1 단방향 미러라 자체 채움은
  원칙 위반. "준비 중" 배지가 이미 정직한 상태 표기 (memory: project-rahap1-info-source)
- **P2-8 발송 로그**: NHN 키 없이도 가능했음 (정정) — kakao.py 가 stub 발송도
  AuditLog(action=alimtalk.send) 에 기록 중. GET alimtalk-logs endpoint + 알림 설정
  페이지에 최근 7일 로그 카드 (stub = "미발송(기록만)" 표기)
- **P2-10 페어링 TTL**: 기존 `_migrate_*` 패턴 활용 (정정 — 마이그레이션 인프라가
  이미 base.py 에 있었음). `pairing_code_issued_at` 컬럼 + 72h 만료 — 목록은 만료
  코드 숨김+flag, 페어링은 CODE_EXPIRED 거부, 재발급 시 TTL 리셋. 만료·재발급 경로
  smoke 통과. + social_accounts 드리프트 보정도 정식 마이그레이션으로 승격 (프로덕션
  배포 안전)

### 실행 결과 요약 (2026-06-11)
- **P0**: 약관 8개 섹션 본문(앱 TermsScreen + 웹 /terms) / 탈퇴 DELETE /member/me (18종
  삭제, roundtrip smoke 통과) / 고객센터(웹 footer·로그인·온보딩 완료 화면 + 앱 라벨·응대시간) /
  내부용어 정리(mock·Phase 2·내부 경로 — 8개 파일) / 알림톡 미활성 고지(온보딩·알림설정)
- **P1**: 계약 발송 5단계 가이드 / 플랜 예시 3종+일괄 생성 버튼 / QR 디스플레이 설치 안내
  (기본 카메라 명시) / 페어링 코드 카피 정정("만료"→"없음·재발급", 1회용 안내) / **거절
  회원 재신청 경로 신설**(rejected→pending, smoke 통과) + 앱 거절 고지 박스 / 카피
  다듬기(용어 "수업" 통일·죽은 경고 제거·검색 힌트) / 락커 옛 주석 정정 / 스토어 listing.md
  + 공개 /privacy URL
- **P2 완료분**: P2-3 공유 버튼 숨김 / P2-4 KPI 툴팁 6종 / P2-5 스케일 예시 / P2-6 상태
  안내 ⓘ / P2-7 동작 라이브러리 활성(GET endpoint + 60동작 렌더, Benchmark 는 시드 0건이라
  숨김 유지) / P2-9 공개 랜딩 "/" 신설 / P2-11 footer 버전 정리
- **보너스**: copy_lint 기존 부채 전부 해소 (금지용어 3건 + fontSize 3파일) — 앱 테스트
  110개 전부 green (inbox hang 2건 제외는 유지)

---

## 1. 🔴 P0 — 실사용자를 받기 전 필수 (합계 ≈ 1일)

### ☐ P0-1. `[C]` 이용약관 본문 작성 + 화면 신설 — 2h
- **근거(실측)**: `lib/features/auth/signup_screen.dart:255` — 약관 링크의 본문이
  `"이용약관 본문은 정식 출시 시 업데이트됩니다."` 1줄. 결제(회원권)·전자계약 서비스가
  약관 없이 가입을 받는 상태. 스토어 심사 반려 사유이자 분쟁 시 방어 근거 부재
- **작업 단계**:
  1. 약관 골격 초안 작성 — ①서비스 정의(박스 운영 SaaS) ②계정·가입·탈퇴
     ③3자 관계(FACING은 플랫폼, 회원권 판매·환불 주체는 박스) ④전자계약의 효력(전자서명법 §3)
     ⑤포인트·게이미피케이션은 박스 재량 ⑥금지행위 ⑦면책 ⑧준거법·관할
  2. `terms_screen.dart` 신설 (privacy_screen.dart 패턴 복제 — sectionLabel + _Bullet)
  3. signup_screen 의 placeholder 다이얼로그 → 화면 push 로 교체
  4. 웹 어드민 로그인 하단에도 동일 약관 링크 (사장도 당사자)
- **DoD**: 앱 가입 화면 → 이용약관 탭 → 8개 섹션 본문 렌더. copy_lint 통과. "정식 출시 시
  업데이트" 문구 0건 (베타 고지 푸터는 유지 가능: "본 약관은 베타 운영 기준이며 변경 시 공지")
- **리스크**: 법무 검토 전 초안 — 베타 고지로 커버. 검토는 사용자 일정에 따름

### ☐ P0-2. `[C]` 계정 탈퇴 서버 삭제 구현 (방침-동작 불일치 해소) — 반나절
- **근거(실측)**: `lib/features/mypage/privacy_screen.dart:131` TODO — 탈퇴 버튼이
  `prefs.clear()` 만 수행, 서버 데이터 잔존. 방침 화면은 이미 "서버·로컬 모든 데이터 영구
  삭제"라고 고지 → 고지-동작 불일치 (개인정보보호법 리스크)
- **작업 단계**:
  1. 백엔드 `DELETE /api/v1/member/me` 신설 — device_hash 본인 확인 → profile·grade·
     WOD history·membership·points·attendance 삭제
  2. **계약서는 삭제 대상에서 제외** — 서명 완료 계약은 박스 보호를 위해 법정 보존
     (방침 RETENTION 절과 이미 일치). member_id 비식별화(이름 스냅샷은 variables 에 보존)
  3. 앱 `_confirmDelete` 에 API 호출 추가 (실패 시 "잠시 후 재시도" + 로컬 정리 중단)
  4. pytest 1건 (탈퇴 후 각 테이블 행 검증)
- **DoD**: 탈퇴 실행 → 서버 DB 에서 위 6종 행 삭제 + 계약만 잔존(비식별) 확인 쿼리 통과
- **대안(축소판, 10분)**: endpoint 없이 방침 문구를 "탈퇴 요청은 카카오톡 상담 접수 후 7일 내
  처리"로 정정. 단 UX 후퇴라 비권장
- **자기반박**: "베타라 P1 로 미뤄도 된다"는 반론 가능 — 그러나 불일치는 사용자 수와 무관하게
  존재하므로 P0 유지

### ☐ P0-3. `[C]` 고객지원 진입점 — 웹 어드민 0개 해소 — 1h
- **근거(실측)**: `web/facing-admin/templates/_layout.html:107-110` footer = 버전 + 로그아웃뿐.
  전 22페이지에 문의 경로 없음. 앱은 Profile 의 카톡 버튼 1개 (라벨 "카카오톡 상담")
- **작업 단계**:
  1. 웹 footer 에 "문의 — 카카오톡 채널" 링크 (`pf.kakao.com/_kxbxanX/chat` — 앱과 동일 채널)
  2. 앱 버튼 라벨 "고객센터 (카카오톡)" 로 변경 + 부제 "평일 10–18시 답변" (실제 응대 가능
     시간은 사용자 확정 필요 — 임시값으로 넣고 표기)
  3. 온보딩 위저드 완료 화면에도 "막히면 문의하기" 1줄
- **DoD**: 웹 전 페이지 footer 에서 카톡 채널 새 탭 오픈 / 앱 라벨 변경 렌더 확인
- **연계**: 응대 시간 문구는 `[U]` 사용자 확정 1건 포함

### ☐ P0-4. `[C]` 사용자 화면의 내부 용어 노출 정리 — 30m
- **근거(실측)**: `lib/features/mypage/algorithm_screen.dart:68` — "(Phase 2 확정 — 현재 mock)"
  이 회원에게 노출. 같은 화면 :105 "Split 시뮬레이터는 Phase 2 예정." 은 기대 형성용으로 유지 가능
- **작업 단계**:
  1. :68 → "Scaled 기록 가중치는 베타 기간 중 보정됩니다" 로 교체 (내부 상태는 코드 주석으로)
  2. 사용자 노출 문자열 전수 grep: `mock|TODO|Phase 2|FIXME` — Text() 내 잔존 0건 확인
     (Phase 2 는 기대 형성 문맥 2건만 화이트리스트)
- **DoD**: grep 결과 화이트리스트 외 0건 + copy_lint 통과

### ☐ P0-5. `[U]`+`[C]` 카카오 알림톡 — 프로덕션에서 stub 으로 도는 문제 (★2차 검증 신규 발견)
- **근거(실측)**: `services/facing/api/notifications/kakao.py:30-32,93-101` — NHN Bizmessage
  키 3종(`NHN_BIZMESSAGE_APP_KEY/SECRET_KEY/SENDER_KEY`) 미설정 시 **stub 분기** (로그만 찍고
  발송 안 함). 금일 Railway 변수 실측에서 **NHN 키 3종 전부 없음** → 프로덕션도 stub.
  그런데 사장 온보딩 위저드(STEP 3)는 "카카오 알림톡 8 템플릿"을 동작하는 기능처럼 소개 — 과대약속
- **작업 단계**:
  - `[U]` 정공법: NHN Cloud Bizmessage 가입 + 발신프로필 등록 + 키 3종 발급 → Railway 등록.
    가이드 문서 이미 존재: `services/facing/notifications/NHN_BIZMESSAGE_SIGNUP_GUIDE.md`
    (단, 사업자등록·채널 심사 수일 소요 — 론칭 전 완료 어려울 수 있음)
  - `[C]` 단기 보정(키 전까지): 온보딩 위저드·알림 설정 페이지에 상태 배지 — "알림톡은 발신
    프로필 등록 후 활성화됩니다 (현재 준비 중)". stub 상태를 사장이 인지하게
- **DoD**: (단기) 알림 설정·온보딩에 미활성 고지 노출 / (정공) 키 등록 후 실발송 1건 + 발송 로그
- **자기반박**: 1차 감사는 이 항목을 아예 누락 — 온보딩 위저드의 약속과 인프라 상태를 교차
  검증하지 않았음. 2차에서 Railway env 실측과 결합해 발견

---

## 2. 🟠 P1 — 첫 박스 온보딩 전 (합계 ≈ 1.5일)

### ☐ P1-1. `[C]` 계약 발송 5단계 가이드 (웹) — 1.5h
- **근거**: `contracts.html:150` 빈상태 = "계약서 없음. + 계약서 발급 으로 작성하세요." 1줄.
  온보딩 위저드도 계약은 "QR 서명·PDF 자동 저장" 1줄뿐. 사장의 실제 질문 순서를 못 받쳐줌
- **작업**: 계약 0건 빈상태를 5단계 카드로 확장 —
  `① 템플릿 작성(계약서 템플릿 메뉴) → ② 발급(회원 선택+변수 입력) → ③ 발송 → ④ 회원 앱
  Profile→Contracts 에서 서명 → ⑤ 서명 완료 후 PDF 다운로드·QR 검증`. 각 단계에 해당 화면 링크
- **DoD**: 계약 0건 박스에서 5단계 렌더 + 각 링크 정상 이동

### ☐ P1-2. `[C]` 회원권 플랜 예시 템플릿 — 1h
- **근거(2차 정정)**: `settings_plans.html` CRUD 는 **완전 구현** (1차 "빈 폼" 판정 과장).
  부족한 건 "뭘 만들지" 예시뿐
- **작업**: 플랜 0개 빈상태에 예시 3종 카드 (1개월 150,000 / 3개월 390,000 / 6개월 690,000,
  "예시 — 가격은 박스 기준으로 수정") + "예시로 시작하기" 버튼(3건 일괄 생성)
- **DoD**: 플랜 0개 상태에서 예시 노출 + 일괄 생성 동작

### ☐ P1-3. `[C]` QR 디스플레이 설치 안내 — 30m
- **근거**: `checkin.html:8` 헤더 1줄뿐. "회원은 폰 **기본 카메라**로 스캔" (앱 내 스캐너 아님)
  이 어디에도 없음 — 현장에서 가장 헷갈릴 포인트
- **작업**: 페이지 하단 접이식(details) 3줄 — 거치 권장(태블릿/모니터), 전체화면 버튼 위치,
  기본 카메라 스캔 안내
- **DoD**: /checkin 하단 안내 렌더

### ☐ P1-4. `[C]` 코치 페어링 코드 안내 — 카피 정정 (★2차에서 사실관계 뒤집힘) — 30m
- **근거(2차 실측)**: `api/admin.py:1689` — 코드는 **TTL 없는 1회용** (사용 시 폐기, 만료 시간
  없음). 그런데 `coaches.html` 에 "코드 만료" 라벨 존재 → 백엔드 사실과 불일치하는 카피
- **작업**: 1차 권고였던 "유효 N시간 표기"는 **폐기**. 대신 ① coaches.html "코드 만료" 라벨의
  발생 조건 확인 후 사실대로 정정 ("미사용" / "사용 완료") ② 발급 모달에 "코드는 1회용 —
  코치가 입력하면 소멸, 분실 시 재발급" 1줄
- **DoD**: 라벨·안내가 백엔드 동작과 일치 (§0-B 정신)
- **보안 메모**: TTL 없는 영구 유효 코드는 유출 시 리스크 — TTL 도입은 P2 백엔드 과제로 별도 등재 (P2-10)

### ☐ P1-5. `[C]` 박스 가입 거절 시 회원 안내 — 1.5h
- **근거**: 거절 시 앱에서 멤버십이 조용히 사라짐. pending 처리(잠금·안내)는 잘 돼 있으나
  거절만 무안내
- **작업**: ① 백엔드 — 거절 시 멤버십 행을 즉시 삭제하지 않고 status=rejected 1회 노출 후 정리
  (또는 거절 사유 쪽지 자동 발송 — 기존 쪽지 인프라 재사용이 더 쌈) ② 앱 — rejected 감지 시
  "가입이 승인되지 않았습니다. 박스에 문의하거나 다른 박스를 찾아보세요." + 박스 검색 버튼
- **DoD**: 거절 시나리오 에뮬레이터 재현 — 안내 1회 노출 → 확인 후 정리

### ☐ P1-6. `[C]` 카피 다듬기 일괄 — 1h
| 위치 | 현재 | 작업 |
|---|---|---|
| `onboarding_benchmarks.dart:334` | "Calc failed. Retry." (유일 영어 에러) | V7 룰 유지 여부 결정 후 통일 |
| `gym_search_screen.dart:140` | "검색 결과 없음" | + "박스명 일부만 입력해 보세요" |
| 웹 수업/클래스 혼용 | classes.html "수업 관리" ↔ wod.html "클래스 예약" | 화면 표기 "수업" 통일 |
| `announcements.html:110,120` | 자체 "endpoint 미구현" 경고 잔존 (2차: **false alarm** — 실구현됨) | 죽은 경고 문구 제거 |
- **DoD**: 4건 반영 + 웹 전 템플릿 "클래스" 표기 grep 0건 (코드 식별자 제외)

### ☐ P1-7. `[C]` 락커 페이지 옛 주석 정리 (★2차 정정 — 1차 오판) — 15m
- **근거(2차 실측)**: 1차 "백엔드 Phase 1.5 미구현" 판정은 `app.py:191` **옛 주석** 기반 오판.
  실제는 `api/admin.py:2003,2036` GET/POST lockers endpoint + 프론트 배정·회수·일괄추가
  완전 구현 (lockers.html 279줄)
- **작업**: app.py 의 사실과 다른 주석 제거 + 락커 빈상태에 "일괄 추가로 시작 (존·번호 범위)"
  1줄 — 기능은 손대지 않음
- **DoD**: 주석-실제 일치. 빈상태 안내 렌더

### ☐ P1-8. `[U]`+`[C]` 스토어 등록 패키지 — 반나절 (조건부: Play Store 등록 결정 시)
- **근거**: 스크린샷·설명문·그래픽·**방침 웹 URL**(스토어 필수 — 앱 내 화면으로 불충분) 전무
- **작업**: `[C]` 짧은 설명 80자 + 전체 설명 + 에뮬레이터 스크린샷 6장(금일 검증 화면 재활용:
  인트로/홈/WOD/예약/서명/Tier) + 1024×500 그래픽(아이콘 시스템 확장) + 방침 정적 페이지 1장
  (facing-admin 에 /privacy 라우트 추가가 최저비용) / `[U]` Play Console 계정($25)·등록 진행
- **DoD**: 등록 자료 일체가 `docs/store/` 에 준비 + /privacy 공개 URL 200
- **자기반박**: APK 직배포(MVP 계획)면 본 항목 전체가 P2 강등 — 배포 방식 결정이 선행

---

## 3. 🟡 P2 — 론칭 후 2주 (우선순위순)

| # | 항목 | 근거(실측) | 작업 요지 | 예상 |
|---|---|---|---|---|
| ☐ P2-1 | `[C]` 앱 FAQ 화면 | 지원 채널 = 카톡 단일 | **실문의 누적 후** 상위 10문답 작성 (추측 작성 금지) | 2h |
| ☐ P2-2 | `[C]` 재활 "준비 중" 부위 | rehab comingSoon 플래그 다수 (rehab_screen.dart:131,256) | 콘텐츠 순차 채움 or 미완 부위 숨김 결정 | 부위당 2h |
| ☐ P2-3 | `[C]` Tier 카드 공유 | onboarding_grade.dart:93 "Phase 2 지원 예정" 토스트 | 구현 전까지 버튼 숨김 (정직성) | 30m |
| ☐ P2-4 | `[C]` 웹 KPI 정의 툴팁 | stats.html KPI 6개 정의 설명 없음 | hover 툴팁 ("활성회원 = 승인+미만료+미정지") | 1h |
| ☐ P2-5 | `[C]` WOD 스케일 가이드 예시 | 입력칸만 존재 | placeholder "RX 43kg / Scaled 30kg" | 15m |
| ☐ P2-6 | `[C]` 회원 상태 플로우 안내 | 대기→활성→만료→탈퇴 도식 없음 | members 페이지 ⓘ 팝오버 1개 | 1h |
| ☐ P2-7 | `[C]` Benchmark·동작 라이브러리 탭 | wod.html:15-18 주석 숨김 (시드는 완료, GET endpoint 만 부재) | endpoint 1개 + 주석 해제 | 반나절 |
| ☐ P2-8 | `[C]` 알림톡 발송 로그 | 설정만 있고 성공/실패 확인 불가 | 최근 7일 발송 로그 탭 (P0-5 키 등록 후 의미) | 반나절 |
| ☐ P2-9 | `[C]` 전용 마케팅 랜딩 (E-17) | 어드민 로그인이 사실상 첫인상 | workcheck-web 패턴 1페이지 | 1일 |
| ☐ P2-10 | `[C]` 페어링 코드 TTL 도입 | 현재 무기한 1회용 (P1-4 메모) — 유출 리스크 | 발급 후 72h 만료 + 라벨 연동 | 2h |
| ☐ P2-11 | `[C]` 웹 footer 버전 drift | "v0.4 · Phase 2 (베타)" ↔ 앱 1.0.0 | 론칭 시점 표기 통일 (§0-B) | 10m |

> 1차 P2-12(온보딩 완료 직후 안내)는 검토 결과 **드롭** — Tier 공개 → 셸 진입 시 "5 TABS"
> 코치마크가 이미 같은 역할 수행 (중복 안내는 Skip 율만 올림).

---

## 4. 이미 완성 — 작업 불필요 (2차 검증 확정)

- 웹 어드민 21개 템플릿 중 19개 완전 구현 — 1차 "미완성 다수" 인상은 **부분 독해에 의한 오판**
- 온보딩 위저드 약속 8건 중 7.5건 실구현 (Benchmark 탭 0.5 + 알림톡은 키 문제로 P0-5)
- 야간 발송 금지(21–08시, 정보통신망법 §50)·전자서명법 §3 고지·CSV 일괄·락커·통계 전부 실구현
- 앱: 온보딩 파이프라인·용어사전 20개·Algorithm 투명성 화면·빈상태 카피 — 상위권 수준

## 5. 의존성 그래프

```
P0-1 약관 ─────────────┐
P0-2 탈퇴 endpoint ────┤→ (전부 독립 — 병렬 가능)
P0-3 지원 진입점 ──────┤
P0-4 mock 문구 ────────┘
P0-5 알림톡: [U] NHN 가입(수일) ∥ [C] 단기 고지(즉시) — 단기 고지를 먼저
P1-8 스토어: 배포 방식 결정([U]) 이 선행 게이트
P2-8 발송 로그: P0-5 키 등록 후 착수
나머지 P1·P2: 상호 독립
```

## 6. 자기반박·정정 기록 (1차 → 2차)

| # | 1차 판정 | 2차 검증 결과 | 교훈 |
|---|---|---|---|
| 정정1 | 락커 = 백엔드 미구현 (Phase 1.5) | **오판** — endpoint·프론트 모두 완전 구현. app.py 옛 주석을 사실로 믿음 | 주석 ≠ 사실. 라우트 grep 으로 교차 검증 의무 |
| 정정2 | 웹 어드민 placeholder 다수 | **과장** — 의도 숨김 1건(Benchmark) + 죽은 경고 1건뿐 | 파일 앞부분만 읽고 일반화 금지 |
| 정정3 | 페어링 코드 "TTL 표기 추가" 권고 | **전제 오류** — TTL 자체가 없음(무기한 1회용). 표기가 아니라 카피 정정+TTL 도입(P2) 이 맞음 | 권고 전 백엔드 동작 실측 |
| 신규 | (누락) | **알림톡 prod stub** — 코드는 env 분기 실구현인데 Railway 에 NHN 키 0개. 온보딩 약속과 불일치 = P0-5 승격 | 기능 약속 ↔ 인프라 env 교차 검증이 콘텐츠 감사의 일부 |
| 한계 | — | 2차 "완전 구현" 판정도 **정적 코드 기준** — 실호출 검증은 Phase 5 smoke 가 커버한 범위(예약·체크인·계약·SSE) 외 페이지(락커·포인트 설정·알림 설정 CRUD)는 미실행. 해당 페이지 작업 착수 시 실호출 1건씩 선행 | 정적 분석의 신뢰 한계 명시 |
