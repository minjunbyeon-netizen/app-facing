# 오버나이트 체크리스트 — 2026-08-20 23:08 사용자 일괄 승인

> 지시 원문 요지: 남은 기능 전부, 모두 승인, 완료 즉시 커밋·푸시·배포,
> 에뮬레이터 재빌드, 최종 골든 마무리, 끝까지 무한 반복 (/loop).
> 전부 끝나면 1시간마다 업그레이드 버전 (커밋 태그 1v·2v·3v… — 내일 버전 비교용).
> 진행 규칙: blocker 는 메모 박고 다음 항목 (2026-05-26 오버나이트 방침).

## 체크리스트 (순서 = 의존도·리스크 순)

- [x] C1. PC→회원 쪽지 발신 — 회원 쪽지 화면 [+ 새 쪽지] (회원 선택+제목/내용),
      코치 발신 API 배선 (기존 GymCoachNote 재사용)
- [x] C2. 회원 쪽지 업그레이드 — 대화 헤더에 회원 정보(이름·레벨·회원권 D-day)
      연동 + 회원 상세 바로가기 (글자 크기·영어판은 스코프 제외)
- [x] C3. 수업 관리 캘린더 30분 단위 슬롯
- [x] C4. 수업 안내(템플릿)·배치·수업 관리 연동 — 수업 등록 시 템플릿 선택하면
      제목·내용 자동 완성 (배치 내용 인계)
- [x] C5. 회원권 설정 — 삭제 즉시 목록 반영 (리로드 확인·수정)
- [x] C6. 통계 — 회원권 등록·결제 → 매출 연동 검증 + 날짜 프리셋
      (이번 주 / 이번 달 / 저번 달 / 직접 선택)
- [x] C7. 일정 달력 — 월 캘린더 그리드로 공지·이벤트 표시 (간트 → 월 뷰,
      먼 일정 보기 개선)
- [x] C8. 포인트 설정(적립 규칙) — 수정 기능 + 적용 실검증
- [x] C9. 업적 설정(리워드 규칙) — 수정 가능 항목 재확인·수정 UI 보강 + 적용 검증
- [ ] C10. 클릭-수정 갭 스윕 — 표시만 되고 수정 진입 없는 곳 전 화면 점검·보강
      (수업 안내 행·배치 행·공지 행 등)
- [ ] C11. 앱 영향분 반영 + 에뮬레이터 재빌드 실검증
- [ ] C12. 최종 — 백엔드 pytest·앱 테스트·골든 갱신·lint·전체 배포·보고

## 완료 로그
- 23:14 C1+C2 — 새 쪽지 발신·헤더 연동 (backend members-brief 신설), playwright 왕복 검증, 양 repo push

- 23:5x C3~C9 완료 — 30분 슬롯·배치 자동완성·회원권 삭제 표시·통계 프리셋·월 달력·포인트 규칙 검증·리워드 규칙 수정 모달. 배치 행 수정(C10 일부)까지. 1차 railway 배포 2건 업로드.

## Blocker 메모
(멈추지 말고 기록만)

## 3라운드 — 사용자 10건 지시 (2026-08-21 08:40 수신)
> 완료 후 playwright 로 **역순(R10→R1)** 재검증 의무.

- [x] R1. 설정-락커 탭 빈 상태 CSS 깨짐 — "등록된 락커 없음…" 문구 스타일 SSOT 정렬
- [x] R2. 회원 탭 표기 불일치 — 사이드바 "유료" vs 메인 "활성" → API 대조 후 1:1 이름 통일
- [x] R3. 회원쪽지 — 대화창에 회원 이름 표시 + PC에서 코치 선발신 확인·보강
- [x] R4. 좌측 사이드바 — "가입관리" 별개 탭 분리
- [x] R5. 수업관리 30분 단위 경계선 — 굵은 줄 제거, 배경 톤에 맞게 재디자인
- [x] R6. 수업관리 반복 스케줄 — "매주 평일 06:00 / 토 10:00" 식 반복 규칙 설정탭 + 개별 날짜 override
- [x] R7. 공지 오염 — 앱에 "달리기인증완료" 노출. 공지 API 와 알림 배선 분리 (코치 등록분만 공지)
- [x] R8. 회원권 설정 — 비활성보기 → 다시 숨기기 시 에러 수정
- [x] R9. 포인트 규칙 설정 UX — 조건값 쉬운 말 + placeholder 3종(단순인증·PR비교·승인 시)
- [x] R10. 업적설정 — 인증대기함을 전자계약서식 화면 내 탭으로 분리

### 3라운드 완료 로그 (2026-08-21 09:00~)
- R1 style.css `.locker-grid > .table-msg` 전체 열 스팬 — 빈 상태 120px 셀 갇힘 해소
- R2 사이드바 유료→활성 · stats "활성 회원"→"등록 회원"(다른 집계 다른 이름) ·
  상세 헤더 정지/만료 분기 · 회원권 배지 활성→이용중
- R3 기존 구현 확인 (peer 헤더 이름·레벨·전화·상세 링크 + 새 쪽지 선발신) — 역순 검증 대상
- R4 사이드바 "가입" 그룹 분리
- R5 30분 경계 굵은 점선 = border-top-style 만 지정 → medium 3px 함정. row-gap 0 +
  정시 실선/:30 무경계로 재설계
- R6 백엔드 위임(ClassScheduleRule + materialize) + classes.html 반복 스케줄 모달
- R7 서버 auto_kind="reward:action_log" + 앱 홈 공지 아코디언 소스를 AnnouncementsState 로 교체
- R8 재현 불가(로컬 왕복 에러 0) — 서버 재기동 순단 추정, 실패 시 "다시 시도" 링크 방어 추가
- R9 수정 모달 라벨 "달성 기준 (몇 회/며칠)"+힌트 · 빌더 예시 프리셋 3종(출석·PR·달리기 인증)
- R10 인증 대기함 → 화면 내 탭 분리 (탭 배지 건수)

### 3v (2026-08-21 오전) — 3라운드 10건 전부 반영
- 백엔드: ClassScheduleRule + materialize + auto_kind (163 passed 1 skipped)
- 관리자: R1~R5·R8~R10 (lint 0건) · 앱: 홈 공지 소스 교체 (전 테스트 그린)
- 역순 검증 R10→R1 playwright 완료: 탭·프리셋·토글·반복스케줄(다음주 월~금 06:00
  5슬롯)·30분경계·가입그룹·쪽지 선발신(이름 헤더)·유료→활성·락커 빈상태 풀스팬
- 태그 3v ×3 repo · railway up ×2

### 4v (2026-08-21 09:04~09:20) — 사용자 추가 지시 4건
- 수업 길이 개념 정정: 30분 = 시작 시각 단위. chip 이 duration 만큼 세로 스팬
  (50분 실등록 왕복 검증), 등록 모달에 수업 시간(분) 입력 (템플릿 기본시간 자동).
  함정: 명시 배치 chip + auto-placement 셀 혼용 시 전체 밀림 → 전 셀 명시 좌표
- 페이저 SSOT: .pager-btn 신설 — 수업 관리 비대 화살표 + 일정 달력의 죽은
  .btn-ghost 참조(폐기 클래스) 둘 다 28px 통일
- 업적 설정 재편: 기본 업적 22종을 성격별로 카테고리 1(꾸준함·복귀)/2(기록·PR)/
  3(대회) 카드 안 소절로 분배, 별도 표 폐지
- 활성 체크박스 → .toggle 스위치 (규칙·카탈로그·마스터 전부, 왕복 검증)
- admin 커밋·railway up·4v 태그 ×3 repo

## 버전 재시작 (2026-08-21 09:21 사용자 지시 — "원본 v1 빼고 2가지 디벨롭")
- **v1** = 4v 시점 스냅샷 (원본 마커, 세 repo)
- **v2** = 업적 카테고리 서버 승격 (catalog.category — 시드 _category 정본,
  RULE_* 는 규칙 카테고리) + 수업 칩 분 단위 비례 높이 (50분 83% vs 60분 100%)
- **v3** = 성능: Chrome localhost(::1 선시도) 오리진이 요청당 ~310ms 를 태우던 것
  실측 규명 → 관리자 localhost→127.0.0.1 308 리다이렉트 (API 24-37ms, ~10×) +
  RULE_* category 백필 마이그레이션
- 태그 v1·v2·v3 ×3 repo, railway 배포 (rollback: git checkout v{N})

## 버전 재시작 (2026-08-21 09:21 사용자 지시 — "원본 v1 빼고 2가지 디벨롭")
- **v1** = 4v 시점 스냅샷 (원본 마커, 세 repo)
- **v2** = 업적 카테고리 서버 승격 (catalog.category — 시드 _category 정본,
  RULE_* 는 규칙 카테고리) + 수업 칩 분 단위 비례 높이 (50분 83% vs 60분 100%)
- **v3** = 성능: Chrome localhost(::1 선시도) 오리진이 요청당 ~310ms 를 태우던 것
  실측 규명 → 관리자 localhost→127.0.0.1 308 리다이렉트 (API 24-37ms, ~10×) +
  RULE_* category 백필 마이그레이션
- 태그 v1·v2·v3 ×3 repo, railway 배포 (rollback: git checkout v{N})
