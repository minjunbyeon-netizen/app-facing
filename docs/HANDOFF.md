# HANDOFF - 2026-08-19 12:31

> 주제: 에뮬레이터 실기동으로 골든 41장 적용 상태 전수 체크 완료 + 카피 픽스 4줄.
> 다음 과업은 사용자가 지정한 **G24 2차 (폰 수업 수정 PATCH)** — 착수 전 컨텍스트 99% 로 인계.

## 완료 (이 세션)

1. **골든 게이트 통과** — `flutter test test/golden` 34케이스(41장) 전건 통과, 픽셀 회귀 0.
2. **에뮬레이터 실기 검증 한 바퀴** (Medium_Phone_API_36.1 + 로컬 백엔드 :5060 + `hyphen.db`) —
   회원 측 약 20화면(수업 탭·수업 상세·코치 질문 시트·홈·업적 전체/상세·내 정보 메뉴 전부·
   알림함·결과 입력 시트·로그인 3종) + 코치 측(대시보드·수업 등록 시트·날짜 선택 다이얼로그·
   명단 시트) 전부 골든과 일치. 인계 픽스 2건 실기 확인: 수업 상세 리더보드 유령 행 없음 ·
   날짜 다이얼로그 오늘(19) 정선택(currentDate 픽스).
3. **코치↔회원 풀 사이클 실증** — 코치 수업 등록 → 회원 화면 즉시 노출 → 예약(1/12) →
   취소(0/12 원복, 취소 시 자리 반환 픽스 확인). 실제 백엔드 왕복.
4. **카피 픽스 4줄 + 골든 2장 재생성** — 커밋 `54db2b0` (push 안 함):
   'No coach feedback yet.' → '아직 피드백 없음.' (한글 기본 v1.29 잔재) ·
   'Profile → Privacy Policy'/'프로필' → '내 정보 → 개인정보처리방침' (v3.0 탭명 정본, 3곳).
   `flutter test` 164 전건 통과 · 갤러리 41장 재생성.

## 진행중

- 없음 (G24 착수 직전 인계).

## 대기

- [ ] **G24 2차: 폰 수업 수정 PATCH** — 사용자가 "1" 로 착수 지시. 새 세션 제1 과업.
      정본 = `../../services/facing/docs/SSOT/` 갭대장.
- [ ] 기타 잔존 갭 (전 인계 이월): 리더보드 is_pr 실판정 · 시즌 윈도우 연도 무구분 ·
      CF_QUARTERFINAL require_engine · 온보딩 완료 영속 서버화(현 localStorage).
- [ ] §0-B 잔재: Railway 구명 변수 `FACING_BACKEND_URL`(admin 웹 콘솔) 정리.
- [ ] 상태 변형 5종(에러·미가입·오프라인·이력 에러·승인 대기)은 실기 미재현 — 골든으로만 검증.
- [ ] 로컬 커밋 push — 사용자 "배포해" 명시 시에만 (`54db2b0` + handoff 커밋).

## 결정사항 / 주의

1. **이 repo push 금지** (배포 금지 규칙 v1.16.1) — 이 세션은 로컬 커밋만.
2. **로컬 `hyphen.db` 에 테스트 수업 1건 잔존** (2026-08-19 04:00–05:00 · 정원 0/12,
   예약은 취소해 원복). 다음 실기동 때 "오늘 수업 1건" 으로 보이는 원인 — 지우려면
   코치 로그인 → 오늘 수업 카드 → 명단 시트 → 수업 취소.
3. 검증용 프로세스(에뮬레이터·`flutter run`·백엔드 :5060)는 세션 종료와 함께 죽음.
4. 실기 자동화 함정: 코치/회원 로그인 폼의 "coach"/"····" 표시는 **placeholder** —
   실제 입력이 필요 (탭 후 input text).
5. 기준선 유지: 앱 테스트 164 전건 · 골든 게이트 34케이스 · 백엔드 105 passed 1 skipped
   (`pytest tests` 로만) · 계정 coach/member/admin·1234 · 이 repo dart format 금지 ·
   시각은 appClock 만.

## 관련 파일

| 경로 | 역할 |
|---|---|
| `lib/features/gym/wod_detail_screen.dart` | 코치 피드백 빈 상태 카피 픽스 |
| `lib/features/mypage/{terms,faq,mypage}_screen.dart` | 탈퇴 경로 표기 '내 정보' 통일 |
| `test/golden/goldens/member_09_wod_detail.png` · `member_18_terms.png` | 재생성 골든 2장 |
| `build/goldens_gallery.html` | 갤러리 41장 (미추적 생성물) |
| `../../services/facing/docs/SSOT/` | G24 등 갭대장 정본 |

## 다음 세션 권장 첫 프롬프트

`/resume` — 제1 과업: G24 2차 폰 수업 수정 PATCH (갭대장 참조 후 백엔드·앱 순).
