# HANDOFF - 2026-08-27 23:42

## 완료 (이 세션 — 12:32~23:42, 3 repo 커밋·푸시, 백엔드·PC 웹 배포 LIVE, 갤S22 설치 3회)

- [x] **D61 PC 웹 6건** — 공지·일정 컴팩트(1440×900·1280×800 스크롤 0) · 수업 안내 수정 모달 `<input type=time>` 폭 112→auto(한국어 "오전 06:00" 분 자리 잘림) · 체육관 프로필을 형제 설정 화면 골격(`.page-narrow`+카드 2장) · 회원권 설정 표에 기간제/횟수권 그룹 헤더 · 포인트 설정 "프리셋"→**"적립 항목"** · **알림 문구 체육관별 편집**(백엔드 `gym_notification_settings.settings.templates` JSON 부분 덮어쓰기 · `render_note(gym_id)` · PATCH 검증 `INVALID_TEMPLATE`).
- [x] **D62 설정 9화면 교차검증 → 21건 수리** — 4갈래(용어 이원화·고아 버튼·API 3단 계약·실물 클릭). D61 회귀(마스터 OFF 시 '문구 수정' 잠김) 해제 · 수업 안내 '삭제'를 `settings_plans` 복구 패턴으로 · **실패의 빈 상태 위장 4건**(락커·업적·시간표·계약 발급) · 표기 통일(앱 희귀도 한글·이용중/만료·쪽지함·전자계약서, 백엔드 "클래스"→"수업" 8곳, `{상품명}`→`{회원권}`).
- [x] **D63/D64 4기둥** — 스코프 정본을 **코치↔회원 4기둥**(공지·쪽지·수업 예약·수업 공개+업적)으로 재작성, **3면 CLAUDE.md 대전제 5번**에 추가. 인스타그램·로고 URL 제거(3면, DB 컬럼은 휴면) · 검증된 사문 코드 제거(빈 폴더 3·앱 repository 메서드 6·백엔드 서비스 3·쪽지 그룹 4·초대코드 3·switch-gym). README 대장 22~30.
- [x] **D65 존치 확정** — 락커·통계·결제·WOD 소셜·목표/최고기록/히스토리·전자계약서 **제거 안 함**. 4기둥은 **신규 게이트**이지 기존 제거 근거가 아님(대전제 5번 문구 교체). **재제안 금지.**
- [x] **D66 로그인 1화면 통합 + 내 정보 4건 삭제** — `/signup` 갈림길 폐지(참조 5곳 전환), 가입 신청은 로그인 아래 한 줄 · 목표·FAQ·고객지원·데이터 초기화 삭제(화면 코드까지, `goals/`·`faq_screen.dart`). README 대장 31~35.
- [x] **D67 로그인 고정 레이아웃** — 밀림 4대 원인 제거(`HkBackBar`·`HkNoticeSlot`·`helperText:' '`·`HkButton(busy:)`) · **`docs/DESIGN-SSOT.md §레이아웃 안정성` 신설**(용어 표+4대 패턴 정답) · 헬퍼 `test/golden/layout_stability.dart expectStableAnchorY`.
- [x] **D68 앱 전역 레이아웃 시프트 19건 수정** — 소넷 4명 조사 → 5명 병렬 수리. 홈 4겹(배너 오버레이·공지 예약·업적 스켈레톤·출석 상시) · 가입 폼 6칸 · 수업 상세 4구역(로딩/없음/실패 구분) · 코치 주간·쪽지함·노트 · 내 정보(포인트·회원권·배너 통합). 화면별 게이트 `stability_*_test.dart` **5종 신설**(로그인 포함 6종). 실기 확인 후 **수업 탭 배너를 겹쳐 띄우기로 재전환**(상단 56px 상시 빈 띠 제거).
- [x] 부수: `test/golden/fakes.dart` `/api/v1/member/points` 키 `points`→**`balance`**(서버·앱이 맞고 페이크가 틀림 — 포인트가 `-- P` 로 찍히던 원인).

## 진행중
- [ ] 없음.

## 대기 (사용자 결정 필요 — 코드 손대지 말 것)
- [ ] **앱 배포 방법** — 질문만 받고 답변까지 함. 선택지: (가) APK 파일 직접 배포(무료, 아이폰 불가) (나) **구글 플레이 25달러 1회**(서명키 `C:\dev\keys` 보유, AAB 전환·스토어 소개·스크린샷 필요) (다) 애플 앱스토어 **연 99달러 + 맥 필수**.
- [ ] **아이폰 웹 접속 여부** — Flutter Web 은 기술적으로 가능하나 **3면 대전제 2번("회원용 웹 화면 만들지 않는다")과 정면 충돌**. 제약 4가지(첫 로딩·알림 약함·네이버 로그인 재배선·화면 이원화) 설명 완료. **미답 질문: 아이폰 쓰는 회원이 실제로 있는가.**
- [ ] 예약 슬롯이 **한 줄 기준**이라 내용이 여러 줄 오는 구역(댓글·수업 많은 날·코치 명단 2건+)은 **첫 도착 1회** 늘어남. 완전 0 은 내부 스크롤 필요 — 별건.
- [ ] 홈 최상위 `FutureBuilder` 전면 교체 1건(§레이아웃 안정성 패턴 4) 미처리.
- [ ] 알림 발송 시각 재결정(만료만 15시 배치, 결제·수업취소·해지는 즉시) · 알림 JSON 키 `reservation`(=수업 취소)·`cancel`(=회원권 해지) 개명(마이그레이션 동반).
- [ ] 로컬 DB 검증 잔여물: 비활성 요금제 `XCHK임시권` · 비활성 수업 `XCHK임시수업` · 회원 `member` 포인트 +160P 이력 · 테스트 결제 1건.

## 결정사항 / 주의
- **4기둥 = 신규 기능 게이트.** 기존 기능 제거 근거로 쓰지 말 것(D65). 사용자가 콕 집은 것만 삭제.
- **레이아웃 안정성**: 정본 `docs/DESIGN-SSOT.md §레이아웃 안정성`. 용어는 **레이아웃 시프트/CLS/공간 예약/스켈레톤** 고정(별칭 신설 금지).
- **테스트 함정(필수 기억)**: `pumpWidget` 은 같은 타입 트리에서 State·Provider 를 재사용 → 상태를 갈아도 앞 상태가 남아 **거짓 통과**. 작업자 3명이 각각 걸렸음. 상태마다 트리 버리기(`pumpWidget(SizedBox.shrink())`/`UniqueKey`) + `expect(find.text(...))` 로 상태 확증.
- **배너는 밀지 말고 겹친다** — 거의 안 뜨는 배너에 상시 자리를 주면 빈 띠가 남는다(수업 탭에서 실기로 확인해 되돌림).
- 선택지는 **번호 대신 이름**으로 부를 것(사용자가 하루 두 번 지적 — 메모리 `feedback-name-options-not-numbers`).
- 폰 연결: mDNS serial `adb-R5CT503NB5M-r4Y2MU (2)._adb-tls-connect._tcp` (공백·괄호 → `adb -s "…"` 따옴표 필수). IP:포트 방식은 실패함.
- PC 웹 로그인 제한 **IP 당 5회/5분** — 병렬 작업자 검증 시 세션 재사용 필수.
- 프로드 접촉은 `/health`·`railway up` 만. gym 2 실데이터 오염 금지.

## 다음 세션 권장 첫 프롬프트
`/resume` → 배포 방법(플레이스토어 vs 아이폰 웹) 결정부터. 아이폰 회원 유무가 갈림길.
