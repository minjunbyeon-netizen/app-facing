# HANDOFF - 2026-08-20 19:09

> 주제: 업적 시스템 대개편 하루 — 카피 정리 → 데드코드 삭제 → 업적 설정 연동 →
> 카탈로그 대수술 → **리워드 규칙 엔진(GTM 빌더)** → 컨페티 축하 → 완료 기록
> 시트 개편, 3면 배포·에뮬레이터 3차 실검증까지. **사용자 마지막 메시지 3건이
> 미답 상태로 인계됨 (아래 '대기' 1번이 다음 세션 첫 작업).**

## 완료 (이 세션 — 전부 push·배포 반영)

1. **인트로 검증 + 잔존 카피 7화면 정리** — 인트로 2장은 실물 일치(v2.6 기정리).
   목표 Tier 섹션 삭제·약관/방침/FAQ/세션시트/초기화 문구 현행화.
2. **도달 불가 화면 13파일 삭제** (구 "숨김=코드 보존" 종료) — 대장 =
   `README.md §제거된 기능 대장` (복원 = git log). Attend·Rehab·리더보드·공지
   전용·임포트·체육관 개설/검색·직원 연결·TierBadge.
3. **업적(게임) 설정 — PC 코치 설정 → 회원 폰 연동** — gym_achievement_settings
   (마스터/개별 토글) + 카탈로그 코치 편집(픽토그램 13종 SVG·희귀도·달성 포인트·
   비고 1회/반복) + `/settings/achievements` 신설.
4. **카탈로그 대수술** — Engine/Tier/신체/프리셋 기반 달성 불가 업적 ~70종 삭제,
   시드 22종(기록·PR 계열)만. 정본 = `models/base.py DEAD_ACHIEVEMENT_TRIGGERS`
   (시드 프루닝 + 마이그레이션 동일 목록). 달성 시 포인트 자동 적립.
5. **리워드 규칙 엔진 P1+P2** (`PLAN-reward-rules.md` 승인·구현 / BRIEF §11.11) —
   GTM 문장형 빌더, **카테고리 3분류**(①자동 출석 ②기록 로그 wod/PR ③코치 인증
   custom), 3표(rules/grants UNIQUE 반복 중복 차단/action_logs 1일 1회),
   reward_engine 훅 3곳, 인증 대기함(승인/거절/대리). 확정: 주=ISO 월요일·
   미소급(streak 연속만 인정)·custom 기본 코치 승인.
6. **해금 축하** — 전 등급 컨페티 캐논(하단 양쪽, 2초·60입자·토큰 팔레트) +
   토스트 기본 픽토그램, 노출 2초. 스냅샷 diff 로 서버 훅 해금도 감지.
7. **완료 기록 시트 v3.3 개편** — 수업 내용 인계·최소 입력·동작별 SCALED/RXD
   (RXD=코치 무게 자동, SCALED 만 입력)·**ELITE 선택지 삭제**.
8. **3차 실검증 (에뮬레이터)** — PC 규칙 생성 → 폰 기록 저장 → 50P 적립 +
   RULE_2 해금 + 컨페티 캡처. 배포: railway 2건×2회, 프로덕션 신API 반영 확인.

## 진행중

- 없음 (코드·테스트·배포 전부 그린 상태로 마감).

## 대기 — ★ 다음 세션 첫 작업

- [ ] **사용자 마지막 메시지 3건 답변·설계** (2026-08-20 18:58, 미답):
      ① "1(P3 도전 카드)이 무슨 말이냐" — 커스텀 인증 규칙의 회원 접점
      (홈 카드에 규칙 문장+진행바+[인증하기] 버튼) 설명부터.
      ② **핵심 지적**: 완료 기록이 EMOM 식 일변도면 안 된다 — For Time(시간)·
      1RM 측정일(무게)·AMRAP 등 **수업 유형별 기록 구조**를 나누고, 로그를 놓고
      "전과 비교해 발전했는가"를 측정하는 설계를 제안하라 (PR 비교·같은 수업
      재도전 비교·1RM 추이 등). 계획부터 세워 승인받을 것.
      ③ P4 트리거 확장(예약·결제·생일·연장)은 **"좋네" 승인됨** — ②설계와 함께 착수 가능.
- [ ] P3 잔여: 도전 카드([인증하기]·진행률) 앱 구현 — ①답변 후.
- [ ] 로컬 테스트 데이터 잔존 (달리기·첫 기록 축하 규칙, 기록 1건, 50P,
      검증0819 일체) — 정리 여부 사용자 결정. 프로덕션 무영향.
- [ ] (이월) PC 웹 PR 태그 실기 확인.

## 결정사항 / 주의

1. **배포 승인은 이 세션 한정** — 새 세션은 배포 금지 기본값 복귀 ("배포해" 명시 시에만).
2. 기준선: **백엔드 153 passed 1 skipped · 앱 167 전건 · 골든 42장**. 계정 coach/1234.
3. 리워드 확정 규칙: 주=ISO 월요일 · 미소급(단 streak 는 진행 중 연속 인정) ·
   custom 인증 기본 approve · 규칙 상한 20/체육관 · 반복 재적립 = grants UNIQUE.
4. 업적·칭호 어휘(RX 계열) 유지 방침 지속. ELITE 는 **회원 선택지에서만** 제거
   (백엔드 enum scaled/rx/elite 유지).
5. 시드는 코치 편집 필드(rarity·icon·points·repeat_kind)를 안 덮어씀 · 프루닝은
   전역(gym_id NULL)만 — 커스텀 RULE_* 보존.
6. 알려진 한계: 업적 토스트는 저장 스낵바 다음 순번(큐) · 동작별 무게 자동
   인계는 코치가 라운드 구성으로 게시했을 때만(자유 서술은 전체 SCALED/RXD 폴백) ·
   반복 인증 사진 첨부는 P4(카메라 권한 0 방침).
7. 검증 프로세스(백엔드 :5060·어드민 :8081·에뮬레이터)는 세션 종료와 함께 죽음.

## 관련 파일

| 경로 | 역할 |
|---|---|
| `docs/PLAN-reward-rules.md` | 리워드 엔진 설계 정본 (승인·구현 로그 포함) |
| `docs/ARCHITECTURE_BRIEF.md §11.10~11.11` | 업적 설정·리워드 엔진 등재 |
| `../../services/facing/services/reward_engine.py` | 평가·지급 단일 판독 |
| `../../services/facing/api/reward_rules.py` | 규칙 CRUD·인증·진행률 API |
| `../../services/facing/models/gym_reward.py` | 3표 (rules·grants·action_logs) |
| `../../web/facing-admin/templates/settings_achievements.html` | 카테고리 3분류 빌더 |
| `lib/features/gym/wod_result_sheet.dart` | 완료 기록 시트 v3.3 |
| `lib/features/achievement/{unlock_toast,confetti_overlay,achievement_state}.dart` | 축하·diff 감지 |
| `README.md §제거된 기능 대장` | 삭제 화면 10건 기록 |

## 다음 세션 권장 첫 프롬프트

`/resume` — 대기 1번(미답 3건)부터: ① P3 도전 카드 설명 ② 수업 유형별 기록
구조·발전 측정 설계안 작성(승인용) ③ P4 트리거 확장 착수.
