# HANDOFF - 2026-08-21 14:19

> 갤S22 실기기 검증 완결 + 로컬 커밋 push 세션 인계.
> 직전 인계장 = `docs/archive/HANDOFF-2026-08-21-1331.md`.

## 완료
- [x] **갤S22 실기기 검증 4종 + 추가 1종 전부 통과** (프로드 백엔드 실통신,
      코치 admin/1234 프로드 로그인 성공 — 데모 시드 실재 확인):
      ① 스플래시↔로그인 로고 밴드 픽셀 대조 diff 0건 (y500~1000 band)
      ② 가입 신청 자동 하이픈 (생년월일 19950101→1995-01-01 · 연락처→010-1234-5678)
         + 제출 없이 이름 필드 실시간 빨간 안내
      ③ 코치 대시보드 헤더 생 role 'OWNER' 아닌 '코치' 한글 라벨
      ④ 날짜 다이얼로그 완전 한글 (날짜 선택·8월 21일 (금)·2026년 8월·일월화수목금토·취소/확인)
         + 시간 다이얼로그 한글 (시간 선택·취소/확인)
      ⑤ (추가) 폰 12시간제 전환 후 **오전/오후 한글 토글** 확인 — 실행 중 앱은 설정
         변경 미반영, 재시작 필요했음 (재시작 후 세션 유지). 확인 후 24시간제 원복 완료
- [x] **무선 ADB 재연결**: 페어링 코드만 받고 mDNS 로 IP 자동 발견 (`adb mdns services`)
      → pair → connect. `C:/dev/tools/phone/last-addr.txt` = `192.168.1.103:39405` 갱신
      (⚠ 포트는 폰 재부팅마다 바뀜 — 다음 세션 실패 시 mdns 먼저)
- [x] **push 완료 (사용자 1번 선택으로 명시 승인)**: app-hyphen 11커밋
      (693acc8..9f772e7) · web-hyphen-admin 2커밋 (03d0524..18aae23).
      두 repo 모두 push ≠ 배포 (배포는 railway up 수동) — 프로덕션 반영 없음

## 진행중
- (없음)

## 대기
- [ ] **admin 인라인 정리본 railway up 수동 배포** — GitHub 엔 올라갔으나 실서버
      (web-facing-admin-production-dca4)는 아직 옛 코드. 픽셀 동일 검증된 정리라
      급하지 않음. 사용자 "배포해" 명시 시에만 실행

## 결정사항 / 주의
- 갤S22 는 **제거 후 재설치**했음 (구 설치본과 서명 불일치 — applicationId 개명 이력의
  문서화된 재설치 케이스). 구 앱 로컬 데이터 소실, 프로드 URL 주입 디버그 빌드 설치됨
- 폰·에뮬레이터 모두 코치(admin) 로그인 상태로 잔존 (무해). 폰 시간 형식은 24시간제 원복 완료
- 미검증 잔여: 가입 신청 **실제 제출** 경로 (프로드 DB 오염 방지로 스킵) ·
  다이얼로그 열어둔 채 시스템 설정 변경하는 극단 케이스
- 코치 로그인 화면에 디버그 프리필(coach/····) 존재 — 이번 세션은 지우고 admin/1234 입력함
- 로컬 서버 잔존 가능: 백엔드 5060 · admin 8081 · 골든 갤러리 8123 (이전 세션 기동분)

## 다음 세션 권장 첫 프롬프트
`/resume`
